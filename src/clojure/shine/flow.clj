(ns shine.flow)

(defn testif
  "this a func for if demo."
  []
  (println (str "2 is " (if (even? 2) "even" "odd")))
  )

(defn testfase)

(defn -main
  [& args]
  (testif)                                                  ; if

  )